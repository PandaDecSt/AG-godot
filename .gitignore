# ── Godot 4+ ───────────────────────────────────────────────
# 导入缓存与编辑器状态，重新打开工程会自动重建，不要入库
.godot/
/android/

# 注意：*.uid 文件【必须提交】（Godot 4.4+ 用它稳定追踪资源引用，
# 漏交会导致别人克隆后脚本引用错乱），所以这里不忽略它们。

# ── 本地诊断草稿 ───────────────────────────────────────────
# .test/ 是无 GPU 排查时的中间产物（dump.json / verify.json / 复算脚本）
# 属于可再生成的验算数据，不入库；生成方法见 docs/02-排查方法论
.test/

# ── AI 助手工作记忆 ────────────────────────────────────────
# 本地开发日志，正式文档已整理到 docs/
.workbuddy/

# ── 系统与编辑器杂项 ───────────────────────────────────────
Thumbs.db
desktop.ini
.DS_Store
*.tmp
*.log
